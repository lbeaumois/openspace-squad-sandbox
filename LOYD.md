# LOYD.md — Project Constitution

> Generated by the coding orchestrator's sandbox scanner on 2026-09-18.
> This file gives the coding agent ground-truth context about its sandbox layout.

## Sandbox Layout (CRITICAL)

The orchestrator has cloned both repos into your sandbox:

- `/workspace/source/` — **READ-ONLY reference**. The original codebase to migrate FROM. NOT CLONED — only target is available.
- `/workspace/repo/` — **YOUR WORKING TREE**. The empty target repo branch you commit converted code TO. `coding_file_write` paths default here.

Never write into `/workspace/source/`. Always read source files from there, then write the converted version into `/workspace/repo/`.

## Detected Source Ecosystem

- (no marker files detected — see directory listing below)

## Source Top-Level Layout

```
(source not cloned)
```

## Target Top-Level Layout (empty — you fill it)

```
total 17
drwxr-sr-x 4 root 1000 4096 Sep 18 12:40 .
drwxrwsr-x 4 root 1000 4096 Sep 18 12:40 ..
drwxr-sr-x 8 root 1000 4096 Sep 18 12:40 .git
-rw-r--r-- 1 root 1000   81 Sep 18 12:40 README.md
-rw-r--r-- 1 root 1000  119 Sep 18 12:40 package.json
drwxr-sr-x 2 root 1000 4096 Sep 18 12:40 src
```


## Working Rhythm

1. Read a small set of source files relevant to your current task.
2. Write the converted file(s) into the target repo. Every iteration must produce at least one file write unless the migration is genuinely complete.
3. Verify with `coding_bash` if useful (syntax check, composer validate, php -l, etc.).
4. **COMMIT FREQUENTLY — `git commit` after every logical unit** (each module fixed, each `rector` pass, each cluster of related files). Commits are NOT automatic — only YOUR commits are pushed after each iteration, and only *committed* work is durably saved if the run is interrupted; uncommitted edits are best-effort-recoverable at best. Do NOT batch a whole phase into one commit at the end — that risks losing hours of work (and burning tokens to redo it). One meaningful, conventional commit per breaking-change fix; commit before moving to the next unit.

